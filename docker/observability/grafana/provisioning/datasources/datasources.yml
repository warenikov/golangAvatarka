# Источники данных описаны кодом: настроенные руками теряются вместе с томом
# и не воспроизводятся на другой машине.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 10s

  - name: Jaeger
    type: jaeger
    access: proxy
    url: http://jaeger:16686
